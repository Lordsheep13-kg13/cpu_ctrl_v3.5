# CPU Control v3.5 - Python dependencies
#
# Note: PyGObject (the "gi" module) requires system-level GTK/GObject
# introspection libraries (libgirepository, GTK3 dev headers, etc.) to
# build/run correctly. On Debian/Ubuntu/Mint these come from apt packages
# (python3-gi, gir1.2-gtk-3.0, libgirepository1.0-dev, etc.), NOT from pip
# alone. install.sh handles the apt side; this file exists so pip can
# verify/install PyGObject itself if it's missing.

PyGObject>=3.42.0
